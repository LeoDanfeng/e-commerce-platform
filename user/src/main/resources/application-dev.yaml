server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.0.119:50001/user?serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root@mc
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 100
      minimum-idle: 5
      idle-timeout: 60000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.119:8848
        username: nacos
        password: nacos

  security:
    oauth2:
      authorizationserver:
        client:
          login-client:
            registration:
              client-id: login-client
              client-secret: "$2a$10$Th55TmYNTmFULjxeWJhUEuEhx6u4gyuLH44ZSP6rh0vUXX6etUcfi" #openid-connect
              client-authentication-methods:
                - client_secret_basic
              authorizationGrantTypes:
                - authorization_code
                - refresh_token
                - client_credentials
              redirectUris:
                - http://127.0.0.1:8080/login/oauth2/code/login-client
                - http://127.0.0.1:8080/authorized
              post-logout-redirect-uris:
                - http://127.0.0.1:8080/
              scopes:
                - openid
                - profile
                - message:read
                - message:write
            require-authorization-consent:  true
          messaging-client:
            registration:
              client-id: messaging-client
              client-secret: secret
              client-authentication-methods:
                - client_secret_basic
              authorizationGrantTypes:
                - client_credentials
              scopes:
                - message:read
                - message:write
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:8081/oauth2/jwks
